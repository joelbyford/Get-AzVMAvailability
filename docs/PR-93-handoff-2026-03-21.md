# Handoff Prompt — Post v1.12.4 Release

> **Date:** 2026-03-21 | **PR:** #93 | **Release:** v1.12.4

---

## 1. Context

PR #93 bumped version to 1.12.4 and was squash-merged to main. This releases the
inline function fallback fix from PR #91 so that single-file downloaders no longer
hit a hard `throw` when the `AzVMAvailability/` module directory is missing.

**Scope:** Version bump across 7 locations, CHANGELOG update, review log cleanup
(reorder to ascending chronological, remove 5 duplicates), 3 Copilot findings triaged.

---

## 2. Current State

- **Main branch:** Healthy. Commit `7473c7e`. All CI green. 208 Pester tests pass.
- **v1.12.4 release:** Published on GitHub. Tag and release auto-created by `release-on-main.yml`.
- **Working directory:** Clean except `fix-review-log.ps1` (untracked temp file, needs deletion).
- **No open PRs** for this project currently.

---

## 3. Key Decisions

- **v1.12.4 over re-tagging v1.12.3** — semver best practice.
- **Copilot Finding 2 (Disagree):** Removing exact duplicate entries from the review log
  is data hygiene, not a violation of append-only. Validated via `git show` at 6 commits.
- **Copilot Finding 3 (Deferred):** `psd1` ReleaseNotes describe script behavior — fix
  when module ships to PSGallery in v2.0.0.
- **Empty commit CI workaround:** `release-metadata-guard.yml` doesn't trigger on PR body `edited` event.

---

## 4. Risks / Caveats

| Risk | Impact | Mitigation |
|------|--------|------------|
| `release-metadata-guard.yml` missing `edited` trigger type | PR body changes don't re-run the guard; requires empty commit workaround | Add `types: [opened, synchronize, reopened, edited]` |
| Module/inline function drift | 34 functions exist in both module and inline — no automated drift test | Add Pester test comparing AST of both copies |
| Module psd1 ReleaseNotes inaccurate | Describes standalone script behavior, confuses PSGallery consumers | Fix in v2.0.0 module conversion |
| `fix-review-log.ps1` temp file | Untracked file in working directory | Manual deletion needed |

---

## 5. Next Steps / Follow-ups

### Immediate
- [ ] Delete `fix-review-log.ps1` from working directory
- [ ] Add `edited` to `release-metadata-guard.yml` trigger types (prevents empty commit workaround)

### v2.0.0 Roadmap (Module Conversion)
- [ ] Phase 1-6 function extraction per copilot-instructions architecture plan
- [ ] Gate 349 `Write-Host` calls behind `-JsonOutput` (#65)
- [ ] Pipeline composability — emit `$familyDetails` to pipeline
- [ ] PSGallery-ready `psd1` with accurate ReleaseNotes
- [ ] Drift detection test: module vs inline function copies

### v2.1.0 (MCP Server)
- [ ] 4 tools: `check_vm_availability`, `find_alternatives`, `get_vm_pricing`, `check_quota`

---

## 6. Reference Links

- [PR #93](https://github.com/ZacharyLuz/Get-AzVMAvailability/pull/93)
- [v1.12.4 Release](https://github.com/ZacharyLuz/Get-AzVMAvailability/releases/tag/v1.12.4)
- [Session Notes](./PR-93-session-notes-2026-03-21.md)
- [Copilot Review Log](../artifacts/copilot-review-log.md)
- [CHANGELOG](../CHANGELOG.md)
