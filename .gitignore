# Export files
*.csv
*.xlsx

# PowerShell
*.ps1xml
*.clixml
*.psm1.bak
*.psd1.bak

# Logs
*.log

# Local troubleshooting logs/artifacts (keep local, never commit)
/tools/logs/*
!/tools/logs/.gitkeep

# Session handoff docs (ephemeral AI session artifacts)
docs/SESSION-HANDOFF-*.md

# Personal AI workflow files (local tooling, not project documentation)
copilot-standing-rules.md

# Temp files
*.tmp
*.temp
.tmp_*
*.out
*~
*.swp

# Editor artifacts
.vs/
.idea/

# Test result artifacts
TestResults/
*.trx
*.coverage

# VS Code (allow .vscode/settings.json for shared linter config)
.vscode/*
!.vscode/settings.json
*.code-workspace

# OS generated
.DS_Store
Thumbs.db
Desktop.ini

# Azure credentials (never commit!)
*.publishsettings
*.azureauth
azureauth.json
azureProfile.json
.env
.env.*
*.secrets.*
secrets.*

# Output directories
/output/
/exports/
/reports/
/Project Review Reports/

# Audit script outputs (generated by tools/Invoke-RepoSelfAudit.ps1 — never commit)
/artifacts/
!/artifacts/copilot-review-log.md

# Backup files created by agent workflow (RULE 3) — archived in-place, never commit
*.backup-*

# Session/context text files (AI chat exports, prompt drafts — local only)
*.txt

# Dev folder (keep for local development only)
/dev/

# Contrib (local experiments, not yet part of main project)
/contrib/

# Demo animations (LinkedIn assets, archived locally)
Demo_Animation.*
backups/linkedin-assets/

# GitHub configuration files (settings FOR GitHub, not part of project)
Protect_Main_Branch.json
**/branch-protection*.json

# AI workflow artifacts (local review docs, never commit)
docs/Gemini*
docs/SESSION-HANDOFF-*.md

# Archived internal process artifacts (remediation, proposals, session handoffs)
docs/archive/

# AI toolkit / agent skills installed locally
.github/skills/

# Browser automation artifacts
.playwright-mcp/
